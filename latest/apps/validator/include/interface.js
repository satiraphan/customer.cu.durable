var modules = {
		view : fn.noaccess,
		validate : fn.noaccess,
		dialog_close : fn.noaccess,
		close : fn.noaccess,
		dialog_task: fn.noaccess,
		task : fn.noaccess,
};
$.extend(fn.app,{validator:modules});
