var modules = {
		view : fn.noaccess,
		dialog_edit : fn.noaccess,
		edit : fn.noaccess,
		lookup : fn.noaccess,
		dialog_return : fn.noaccess,
		return : fn.noaccess,
		report : fn.noaccess,
		excel : fn.noaccess,
};
$.extend(fn.app,{repair:modules});
